import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'io.ionic.starter',
    appName: 'albums-mobile',
    webDir: '../../dist/apps/albums-mobile',
    bundledWebRuntime: false,
};

export default config;
