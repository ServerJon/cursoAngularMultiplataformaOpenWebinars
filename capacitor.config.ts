import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'curso-angular-multiplataforma-open-webinars',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
